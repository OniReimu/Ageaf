# Ageaf Host — Environment Variables
# Copy this file to .env and fill in the keys you need.
#   cp .env.example .env
#
# The host loads .env automatically on startup.
# You only need keys for the providers you want to use.

# ── BYOK Provider API Keys ──────────────────────────────────────
# Set one or more to enable BYOK mode.
# The host auto-detects available providers on startup.

# ANTHROPIC_API_KEY=sk-ant-...
# OPENAI_API_KEY=sk-...
# GEMINI_API_KEY=
# XAI_API_KEY=
# GROQ_API_KEY=
# MISTRAL_API_KEY=
# OPENROUTER_API_KEY=

# ── BYOK Overrides (optional) ───────────────────────────────────
# Force a specific provider or model instead of auto-detection.

# AGEAF_PI_PROVIDER=openai
# AGEAF_PI_MODEL=gpt-4o

# ── Mock Mode (development only) ────────────────────────────────
# Enable mock responses without real API calls.

# AGEAF_PI_MOCK=true
# AGEAF_CLAUDE_MOCK=true
# AGEAF_CODEX_MOCK=true

# ── Host Settings ────────────────────────────────────────────────
# PORT=3210
# HOST=127.0.0.1
